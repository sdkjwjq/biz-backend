# 便利操作第三批及最终交付（2026-09-19）

## 结果

审核中心待办弹窗新增“提交并审核下一条”，保留“确认提交”。两个按钮都提交本次选择的通过或驳回结果，新打开的下一条仍需人工审核。

- 提交前固定对象、意见、结果及完整筛选顺序；使用“业务类型＋审核单 ID”区分记录。
- 成功后立即通知侧栏刷新，移除当前记录，再重新获取待办；从原位置向后寻找仍待处理的记录，支持跨原分页边界，不回到开头。
- 保留搜索、类型筛选及合法页码；无后续待办时关闭弹窗并提示“后续待办已处理完”。
- 提交失败保留对象和意见；提交成功但列表刷新失败时显示独立提示和“刷新列表”按钮，不重复提交。
- 双击只产生一次提交；刷新期间关闭弹窗、改变筛选或离开页面，均取消自动继续。旧响应不能覆盖新的列表或账号状态。
- 原“确认提交”继续保持成功后关闭弹窗的行为。

本批仅修改 `biz/src/components/Audit.vue`，不修改后端业务接口、数据库及审批规则。原有 Audit.vue 历史状态过滤改动单独保留，未混入提交。

## 验证

Playwright / Edge **10 组全部通过，页面异常 0**：

1. 真实任务和成果通过、驳回；跨业务相同审核单 ID 不混淆。
2. 真实绩效从第 10 条继续到原第二页末条，结束后不回到之前记录。
3. 共 15 条真实绩效审核，侧栏待办实时归零；核对后端审核日志。
4. 注入提交失败保留意见；模拟下一条已被他人处理，自动跳过。
5. 注入提交成功后的刷新失败，刷新恢复且没有再次提交。
6. 重复点击只提交一次；刷新期间关闭弹窗取消自动继续。
7. 搜索与业务类型取交集，不跳出筛选结果。
8. 刷新期间注入筛选变化，取消自动继续。
9. 刷新期间通过站内路由离开，旧响应不重新打开弹窗。
10. 原确认提交行为回归。

前三组使用隔离库真实业务接口；异常及并发情形使用明确标记的 Playwright 注入响应。任务退回会软删除审核单，现有日志接口不提供被软删除的审核单，因此任务驳回通过真实提交成功及待办移除核验；绩效、成果和任务通过另核对审核日志。

最终检查：现有 **40 项后端隔离回归全部通过**；Audit.vue ESLint、Git 空白检查、前端生产构建、后端打包均通过。前端保留原有大包体积提示。

脚本：`scripts/ui-audit/convenience-batch3.cjs`。启动隔离服务使用 `--fixture continuous --frontend-port 15273`。所有数据均为合成数据，随机测试库及临时服务已清理；用户的 5173 / 8080 服务继续运行。

证据：[检查结果](evidence/convenience-batch3/results.json)、[审批按钮](evidence/convenience-batch3/continuous-approval-buttons.png)、[待办清空](evidence/convenience-batch3/all-performance-completed.png)。

## Git 与三批交付范围

第三批前端提交：`becb1d6`。本仓库另行提交隔离夹具、Playwright 脚本和本交付记录。

七项便利操作至此完成：返回列表定位、详情章节跳转、复制详情链接、快捷筛选、浏览记忆、绩效／成果前后浏览、连续审核。

- [第一批交付](convenience-batch1-2026-09-19.md)：11 组浏览器检查。
- [第二批交付](convenience-batch2-2026-09-19.md)：16 组浏览器检查。
- 本批：10 组浏览器检查。前两批检查为各自交付时结果，本批最终重新运行了 40 项后端回归。

保留前端原有 Achievement.vue、Audit.vue、vite.config.js 改动，以及后端原有测试、日志和上传文件，不硬重置、不混入功能提交。

## 构建、部署与回滚

最终产物：`target/releases/convenience-final/frontend-dist.zip` 和 `target/releases/convenience-final/biz_backend-1.0-SNAPSHOT.jar`。

产物来自当前工作区，包含原先已存在的未提交前端改动；Git 功能提交只包含本次改动。正式部署时沿用这些已有改动的发布安排。

整套功能先部署后端（第二批摘要接口），再部署前端；保留原配置与上传文件。第三批没有新增后端接口，也无需迁移或回滚 SQL。

仅回滚第三批：在干净发布工作区对前端 `becb1d6` 执行反向提交后重新构建，或恢复第二批前端产物；后端无需变化。整套回滚按各批文档逆序处理，不覆盖已有未提交工作。
