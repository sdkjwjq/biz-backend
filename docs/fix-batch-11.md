# 11-1 修复与本地闭环

前端提交 `b96483e`，仅修改 Works.vue。上传前保存任务 ID、填报值、数据类型、子任务列表、完成情况、原审核单及提交人快照；异步过程始终使用该快照。关闭、切换或卸载后不再清空新详情的表单或覆盖其 loading。新增提交中状态，详情加载和提交期间禁止重复提交。

Playwright + Edge、真实隔离 MySQL 与 HTTP：

- `batch-11-audit.cjs --fixed`：A 填报 3，延迟上传响应后切到 B（7）并填写 B 草稿；A 实际保存 3，B 草稿完整保留。
- `batch-11-audit.cjs --fixed --stay`：不切换页面的正常提交，输入 3，审核单实际保存 3。
- 两场景均无浏览器脚本异常；Works.vue ESLint 和前端生产构建通过，构建保留原有大分包提示。

[结构化测试记录与截图](fix-evidence/batch-11/)。原始缺陷证据未覆盖。复测需全新的 review 隔离环境，先加载 `--batch 11` 种子，再依次执行上述两个场景。

本次不改变退回重提规则及后端校验；四级任务、退回单号按原行为固定快照，未新增这些分支的端到端验证。后端无业务修改，未重复运行后端回归或打包。

产物为 `biz/dist`，未部署；无需数据库迁移。前端仓库执行 `git revert b96483e` 后重新构建即可回滚，或恢复前一版产物。原有未提交改动均保留。

用户授权修复后继续审核，因此同一临时环境继续用于第十二批合成数据取证，整批结束后统一清理服务及随机数据库。
