# 第六、七批修复交付

按用户确认顺序分模块交付，每个模块完成闭环及 Git 记录后等待审核。保留原有未提交改动；第五批、已忽略问题、成果退回重提规则不纳入本轮。

| 模块 | 问题 | 状态 |
| --- | --- | --- |
| 1 绩效详情 | 6-1 审批错对象 | 已修复并验证，用户已确认继续 |
| 2 审核中心 | 6-2 零值、7-1 历史标签、7-2 分页 | 已修复并验证，用户已确认继续 |
| 3 成果数量 | 7-3 小数静默截断 | 已修复并验证，用户已确认继续 |
| 4 数据大屏 | 6-3 卸载后初始化 | 已修复并验证，等待最终审核 |

## 模块 1：绩效详情

### 改动

- 前端提交：`29e05cb`，仅修改 `biz/src/components/Performance.vue`。
- 审核请求使用请求序号和绩效 ID、年份、详情版本共同校验；旧请求的成功、异常及结束回调不能写回新详情。详情版本区分关闭后重开的同一绩效。
- 切换绩效或年份、关闭抽屉、组件卸载时清空审核记录和日志，使旧请求失效。加载期间不提供审批入口。
- 发送审批前复核审核单归属与当前审核权限，固定单号、结果和意见；重复点击被阻止。审批响应只更新发起时仍有效的详情，切换后不会重置其他绩效的表单。
- 审核日志加载沿用相同的失效判断。未改变后端接口、审核状态流转和数据库结构。

### 本地闭环

Playwright、本机 Edge、真实 HTTP、随机 `biz_review_test_*` MySQL 库，全部为合成记录。

| 场景 | 结果 |
| --- | --- |
| A 迟到、当前为 B；实际审批 B | 页面始终为 B 的审核单 971002，实际 POST 单号也为 971002；B 从 10 到 20，A 保持 10 |
| B 审批请求已发出，响应返回前切回 A | A 的审核单保持 971001，输入的“保留 A 的审批意见”未被 B 响应重置 |
| 旧 A 网络失败，B 仍在加载 | B 继续加载且不可审批；仅 B 自己的响应返回后才显示记录和按钮 |
| A→B→A，第二次 A 请求延迟 | 第一次 A 的响应不能结束第二次 A 的加载；重开后仅接受当前请求 |
| 关闭 2026 年详情，切换到 2027 年后放行旧请求 | 2027 年无审核单，仍不可审批；没有混入 2026 年记录 |
| 请求未完成时离开绩效页面，再重新进入 | 旧请求无未捕获异常；重进后可正常查看 B |

执行的 5 个浏览器场景全部通过：`business-audit.cjs performance-race`，以及 `performance-context.cjs` 的 `stale-failure`、`same-record`、`year-change`、`unmount`。

证据：[模块 1 截图和结果](fix-evidence/batches-6-7/module-1/)。`performance-race.json` 保存实际审批单号和 A/B 状态；截图受抽屉滚动范围限制，结构化结果补充完整断言。

- `npx.cmd eslint src/components/Performance.vue`：通过。该命令删除了原有跟踪的缓存文件，已恢复该缓存；后续检查应使用独立缓存位置。
- `npm.cmd run build`：通过，产物位于 `biz/dist`。构建提示部分资源包超过 500 KB；本次没有调整资源拆分。
- JavaScript 语法检查、Git 空白检查通过。本模块没有后端业务改动；整套 21 项后端回归及最终打包在四个模块完成后统一执行。
- 隔离数据库已删除，测试服务 15173/18080 已停止；原有本地 5173/8080 服务保留。

### 复测与回滚

按 `scripts/ui-audit/README.md` 启动 `--fixture business` 环境，先执行实际审批回归一次，再执行四个只读场景。真实审批回归再次执行前需重建隔离库。

手工路径：审核账号登录 → 工作中心“绩效” → 展开“社会效益指标” → 查看 A → 关闭 → 查看 B → 核对 B 的指标与审核单 → 审批 B → 重开 A/B，确认仅 B 流转。模拟慢网络的精确交错使用自动脚本。

回滚前端代码：在 `biz` 仓库执行 `git revert 29e05cb`；或恢复上一版本前端构建产物。无数据库迁移和回滚 SQL。本模块产物用于本地验证，尚未部署。

## 模块 2：审核中心

### 改动及 Git

仅修改前端 `biz/src/components/Audit.vue`，后端接口和数据库保持现状。

| 问题 | 前端提交 | 改动 |
| --- | --- | --- |
| 6-2 | `09f0209` | 任务数据转换保留零；列表及详情仅对 null、undefined、空字符串显示“-” |
| 7-1 | `0af492c` | 标签切换清空旧数据并加载对应接口；请求序号和标签快照阻止旧请求写回；待办数量独立统计，各业务沿用自己的状态码 |
| 7-2 | `db8e86c` | 筛选后每页 10 条，搜索、类型和标签切换回首页，刷新与审批后修正越界页；通知定位继续查完整数据集 |

用户原有的 Audit.vue 历史状态判断改动保留在工作区，未纳入提交；本次提交自身也按任务 40、绩效/成果 30 判断归档状态，不依赖未提交代码。Achievement.vue、vite.config.js 及后端原有改动均保留。

### 本地闭环

使用 `--fixture review`、随机隔离 MySQL 库、Playwright + Edge；除明确标注的兼容性场景，数据均来自真实接口。

| 场景 | 结果 |
| --- | --- |
| 真实绩效零值与非零对照 | 接口 0 显示 0，接口 3 显示 3 |
| 任务列表与详情兼容输入（响应注入） | 数值 0、字符串 "0" 显示 0；null、空字符串、缺省显示“-”；3 不变 |
| 历史标签自动加载 | 切换即请求历史接口并显示 1 条已归档记录，无需刷新；返回待办为空，归档记录未计入待办 |
| 快速往返标签 | 延迟旧历史响应，回到待办后列表保持当前数据，待办统计保持 15，无浏览器未捕获异常 |
| 15 条分页 | 首页 10 条、第二页 5 条，两页不重复，序号连续 |
| 搜索与类型筛选 | 第二页搜索后回首页，匹配 1 条、无匹配 0 条、清空恢复 10 条；成果/绩效类型筛选正确，待办统计仍为 15 |
| 通知定位 | 可直接打开原位于第二页的审核单 972013 |
| 实际审批末页 | 依次审批 972013～972009，均收到真实成功响应；待办 15→10，自动回到第一页且显示 10 条 |

8 个浏览器场景通过。证据：[模块 2 截图和结果](fix-evidence/batches-6-7/module-2/)。响应注入只用于补充零值、空值的前端兼容性，不作为后端输入测试。

ESLint（独立缓存）、前端构建、测试脚本语法及 Git 空白检查通过。构建仍有已有的大资源包提示。没有后端业务代码改动，21 项后端回归按计划在全部模块完成后统一执行。

### 复测、部署与回滚

复测命令见 `scripts/ui-audit/README.md`。末页审批会改变合成数据，必须最后执行；再次运行需重建隔离库。测试结束已停止自建服务并删除隔离数据库，保留原有本地服务。

前端构建产物位于 `biz/dist`，尚未部署。无需更新后端或数据库。回滚代码在 `biz` 中按顺序执行 `git revert db8e86c`、`git revert 0af492c`、`git revert 09f0209`，处理冲突时保留原有未提交改动；也可恢复上一版本前端产物。无需回滚 SQL。

## 模块 3：成果数量（7-3）

### 改动及 Git

- 前端 `b0d6123`：`Achievement.vue` 在数量转换、文件上传和业务提交前校验每条数量为 1～999 的整数。小数保留原输入并给出中文提示，不自动取整；同等次数量继续累加。
- 后端 `501b985`：`AchievementQuantityDeserializer` 仅绑定 `BizAchievement` 九个奖项数量字段，新增、修改共用。拒绝小数、负数、溢出和其他非法类型；数量解析错误由成果控制器返回 HTTP 400 和原有 ErrorVO 结构。
- 接受整数、整数数字字符串、零、缺省、null、数学整数 1.0（含字符串形式）；累计值 1998 及 Integer 最大值可保存。未修改全局数字解析、接口路径、成功响应或数据库结构。
- 原有成果页面未提交改动保留，前端提交仅包含新增的 8 行数量校验。其他用户改动未纳入本次提交。

### 本地闭环

真实 HTTP / 隔离 MySQL：两个新增回归方法均通过。

1. 九字段 × 11 类非法输入 × 新增/修改，共 **198 次**请求全部返回 HTTP 400，中文提示带字段名和整数要求。输入包括数值/字符串小数、负数、溢出、非数字字符串、空字符串、布尔值、数组、对象。每次均比较成果、审核单、审核日志、通知四张表完整内容，确认无变化。
2. 新增/修改分别覆盖 10 组兼容输入、每组九字段，保存后经查询接口核对。包含零、null、缺省、1.0、1998、2147483647。

Playwright + Edge 两个场景通过：输入 1.5 后提交，输入保持 1.5、提示清晰，未发出文件上传或新增请求；改为 2 后真实上传、保存、查询和页面均为 2。空表单、非法文件扩展名及整数对照验证通过。

ESLint、前端构建、后端 `mvn.cmd -q -DskipTests package`、测试脚本语法及 Git 空白检查通过。前端仍有已有的资源包大小提示。完整 21 项旧回归和新增用例按计划在模块 4 完成后统一执行。

证据：[模块 3 截图与测试结果](fix-evidence/batches-6-7/module-3/)。所有测试均使用合成数据；临时数据库已删除，自建服务已停止。

### 部署与回滚

本地前端产物 `biz/dist`，后端产物位于 `biz-backend/target` 的 Spring Boot JAR，尚未部署。部署按后端、前端顺序更新，保留现有外部配置。无需数据库迁移或回滚 SQL。

回滚代码分别执行：前端 `git revert b0d6123`，后端 `git revert 501b985`；或恢复各自上一版构建产物。保留原有未提交改动，不使用硬重置。

## 模块 4：数据大屏（6-3）

- 前端提交 `a181c56`：管理并清理初始化、滚动定时器、时钟及动画；卸载时取消大屏请求。异步返回、nextTick 和图表初始化检查组件及 DOM 有效性，正常取消不显示加载错误。
- 图表数据请求完成后才创建图表，避免等待期间已创建的图表未进入统一清理列表；初始化请求序号阻止旧请求重建图表。
- GET 工具及大屏使用的 API 增加可选配置参数传递 signal；原有无配置调用保持兼容。修改文件为 `Dashboard.vue`、`request.js`、`api/dashboard.js`、`api/achievement.js`。

### 本地闭环

Playwright + Edge 五个场景通过：

| 场景 | 结果 |
| --- | --- |
| 首次统计请求延迟时离开 | 请求报告 ERR_ABORTED，无 invalid dom 或页面错误；重进 5 个图表正常 |
| 800ms 初始化延迟期间离开 | 确认定时器已建立，离开后被清除、未执行；重进正常 |
| 图表初始化数据请求期间离开 | 第二次任务数据请求取消，无图表残留错误；重进正常 |
| 完整进入退出三轮 | 每轮 5 个图表，退出后 DOM 无图表实例标记；最后再次进入正常 |
| 代表成果滚动时离开 | 已脱离 DOM 的列表停止位移；重进正常 |

滚动测试明确注入六条合成归档成果激活滚动，其他请求使用真实接口。延迟初始化测试延长真实 800ms 定时器以确定性地在执行前离开。证据：[模块 4 截图及结果](fix-evidence/batches-6-7/module-4/)。

回滚该模块：前端 `git revert a181c56`，或恢复前一版本前端产物；无需后端或数据库变更。

## 最终验证与交付

- 原有 21 项＋新增数量校验 2 项，合计 **23 项真实 HTTP / MySQL 隔离回归全部通过**，无失败、错误或跳过。
- 七个修改过的前端业务/API/请求文件 ESLint 通过；前端生产构建、后端打包通过。前端仍有原有的超过 500KB 资源包提示，未调整拆包架构。
- 修改的浏览器测试脚本语法、Git 空白检查通过；原始审计证据保留。各模块浏览器回归结果见对应目录。
- 临时数据库及 15173/18080 自建服务已清理，保留用户原有 5173/8080 服务及全部原有未提交改动。
- 产物、校验和、部署及整批回滚步骤见 [最终部署说明](release-batches-6-7.md)。只完成本地交付，尚未部署或推送。

## 原实施约定（已完成）

- 模块 2：标签切换加载对应数据并保护请求时序；待办统计独立于历史记录，状态按任务、绩效、成果区分。保留完整数据供通知定位，筛选后按每页 10 条分页，筛选回首页、刷新修正越界页。
- 模块 3：前端保留非法输入并明确报错，不自动取整；每条 1～999，同等次继续累加。后端九个字段专用严格解析，覆盖新增和修改，拒绝小数、负数及溢出；兼容整数、整数数字字符串、零、缺省、null、数学整数 1.0；不限制累计值为 999。数量解析失败返回 HTTP 400 与 ErrorVO；不改全局数字解析及历史数据。
- 模块 4：GET 工具增加可选配置传递取消信号；清理大屏请求、定时器、动画，异步回调检查组件及 DOM；正常取消不提示错误。
- 每项分别记录 Git，跨仓库提交关联问题编号。全部完成后执行现有 21 项及新增后端回归、前端构建、后端打包和修改文件静态检查，交付部署与回滚说明。无需数据库迁移。
