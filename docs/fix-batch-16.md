# 第十六批修复交付

用户选择：16-1、16-3 修复，16-2 忽略。

## 改动与提交

- **16-1，后端 `df2e221`**：直接提交、四级提交和退回重提统一检查专业群审核人；为空、不存在或已删除时，写入前返回明确错误。沿用现有 ErrorVO/业务错误响应。按已确认账号兼容规则，不根据 status 字段新增登录或审核资格拦截。
- **16-3，后端 `04db2b4`**：任务更新检查父节点及完整祖先链，拒绝自身、后代、循环、不存在或已删除的父节点；改变父节点/层级时校验父子级差、项目和三级任务部门归属，并阻止层级变更破坏已有子任务。层级未变的历史记录不强制重排；一级根节点保留 null/0 表示。

不修改 16-2 的四级列表完整性规则，不改变材料覆盖规则。只改 BizService 和隔离回归测试；前端无需改代码，原有未提交工作保留。接口地址、成功响应、数据库结构不变，无迁移 SQL。

## 本地闭环

1. **审核人专项**：空值、不存在、已删除账号在直接/四级提交均被拒绝；完整核对任务、子任务、绩效、年度值、审核单、快照、日志、通知没有变化。正常四级提交并退回后，移除审核人，直接/四级重提均拒绝且不产生写入；恢复审核人后正常提交、绩效刷新成功。
2. **层级专项**：自引用、后代、无效父 ID、已删除父节点、循环祖先、错误级差、三级任务无父节点均拒绝，数据库保持原样；破坏子任务的层级变更被拒绝。合法换父节点、改名、根节点 null 表示及既有二级根节点改名均通过。
3. **真实 HTTP + Playwright/Edge**：缺审核人提交不再创建审核单，任务保留进行中；自引用更新被拒绝，原父节点仍为 930001，页面任务持续可见。正常更新后刷新仍可见，浏览器 pageerror 为空。
4. 16-2 在审计脚本的修复模式中仅作为“不改既有行为”的对照，依旧使用原始缺陷断言；不表示该项已修复。

完整后端隔离回归 **27 项通过**，失败/错误/跳过均为 0；后端打包、Python/JavaScript 脚本语法及差异检查通过。前端代码未修改，本批未重复构建前端。测试结束后临时服务已停止，专项、完整回归及页面测试的随机数据库均已删除。

[结构化结果和截图](fix-evidence/batch-16/)。原始审计证据保留在 audit-evidence/2026-09-17/batch-16。

复测使用既有随机隔离数据库流程：

```powershell
python scripts/run-review-regression.py taskSubmissionRequiresExistingReviewerBeforeAnyWrite
python scripts/run-review-regression.py taskHierarchyRejectsCyclesAndKeepsValidEdits
python scripts/run-review-regression.py
```

启动全新 `serve.py --fixture review` 后，运行 `batch-16-audit.py --fixed`，再运行 `batch-16-tree.cjs --fixed`。仅用于合成隔离环境。

## 部署与回滚

本次仅后端更新，无前端发布和数据库迁移。后端产物：`target/biz_backend-1.0-SNAPSHOT.jar`；尚未部署。

回滚在干净发布分支依次 `git revert 04db2b4`、`git revert df2e221`，或恢复上一版后端产物；无需回滚 SQL，不硬重置当前工作区。
