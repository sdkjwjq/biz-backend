# 便利操作第一批：浏览定位

## 本批交付

任务、绩效、成果详情新增返回列表定位、快速章节跳转、复制详情链接。定位重新读取权限范围内的列表，展开目录并滚动高亮；目标被筛选隐藏时需用户确认后才清除筛选。未提交表单和已选文件离开前确认，普通关闭按钮行为保持现状。

链接使用现有站内路径，参数分别为 `taskId`、`perfId + year`、`achId + year`；不含 Token。目标不存在或无权限时给出提示。未登录和强制改密流程保留原地址，登录回跳只接受已匹配的站内业务路由。剪贴板不可用时显示可手动复制的链接。旧列表/详情请求不覆盖新链接目标。

第二批快捷筛选、浏览记忆、绩效及成果前后浏览，以及第三批连续审核尚未实施，按约定等待本批审核。

## Git 记录

| 前端提交 | 内容 |
|---|---|
| `2cc2382` | 共用详情工具栏及定位工具 |
| `8e17d18` | 登录、强制改密的安全回跳 |
| `74deef7` | 任务详情接入 |
| `1b0179e` | 绩效年度详情接入及异步保护 |
| `b10945d` | 成果详情接入 |

保留原有成果页、审核中心、Vite 配置未提交改动，成果页仅选择本次补丁提交。

## 验证方法与范围

运行 `scripts/ui-audit/convenience-batch1.cjs`。启动前通过 `serve.py --fixture convenience --frontend-port 15273` 创建全新随机隔离库，仅导入结构和合成数据。登录强制改密使用真实接口，只修改合成账号。

Playwright 检查：登录和强制改密回跳；绩效指定年份链接、复制降级、深层目录定位；成果分类定位；详情内部滚动与任务草稿保护；筛选隐藏后的确认；绩效未提交值保护；无权限链接；记录删除后停止定位；不同角色入口、复制成功及无效成果链接；外站回跳拒绝；同页切换绩效链接后忽略旧关联任务响应。

状态变化、删除列表、剪贴板拒绝和慢响应采用明确的浏览器注入，其余使用真实隔离接口。自动测试不进行业务审批或材料提交。结果及截图在 `docs/evidence/convenience-batch1-2026-09-19/`。

最终结果：11 组通过，浏览器 pageerror 为 0。

修改文件 ESLint、生产构建和提交差异检查通过。本批没有后端业务代码改动；全量后端回归与打包保留到全部三批完成后执行。

## 部署、清理与回滚

仅部署前端 `biz/dist`，无需 SQL 或后端升级。本地 Vite 5173 刷新即可体验；8080 用户后端服务保持运行。隔离测试完成后使用 `target/ui-audit/STOP` 关闭 15273/18080 服务并删除对应随机数据库。

回滚按 `b10945d`、`1b0179e`、`74deef7`、`8e17d18`、`2cc2382` 顺序执行反向提交，或恢复前一版前端产物；不使用硬重置，不影响未提交工作。
