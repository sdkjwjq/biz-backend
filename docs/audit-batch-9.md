# 第九批：消息中心跳转与发送人

前置：8-1、8-3 已修复并分别提交 `98797f9`、`1185535`，测试记录提交 `11c0d83`；8-2 按用户要求忽略。用户授权修复后直接继续审核，本批只新增测试和证据，未修复以下问题。

后续状态：用户确认本批三项全部修复，现已完成；见 [第九批修复与闭环记录](fix-batch-9.md)。下文保留原始审计证据。

| 编号 | 等级 / 类型 | 位置 | 实测问题与影响 | 建议 |
| --- | --- | --- | --- | --- |
| 9-1 | 中 / BUG | `biz/src/components/Audit.vue:820`、`:831` | 点击已归档绩效通知后进入审核中心，提示“暂无与您相关的审批单”，未打开详情；手动切换审批记录可看到同一审核单 972099。定位只在当前待办集合找，导致已归档通知链接失效。 | 通知定位按单号加载对应记录，或待办查不到后查询历史；已归档单以只读方式打开。 |
| 9-2 | 中 / BUG | `biz/src/components/Notice.vue:255`、`:270` | sourceType=1、sourceId=930002 的任务督办通知，仅因主题含“成果”就跳到 `type=achievement&subId=930002`，随后提示没有成果审批单。任务通知被当成成果审核单。 | 优先使用明确的业务类型分流；标题关键字仅用于缺失业务类型的历史兼容。同步梳理数值类型 1/2/3 与字符串类型。 |
| 9-3 | 低 / BUG | `biz/src/components/Notice.vue:68` | 接口实际发送人 fromUserId=910003（合成审核人），详情固定显示“发送人: 系统管理员”。错误标注发送来源，影响联系与责任判断。 | 按真实发送人映射名称，只有明确的系统消息使用系统标签，未知账号使用中性兜底。 |

## 证据与边界

- Playwright + Edge、1440×1100、真实本地 HTTP 与随机隔离 MySQL 库，两个浏览器场景覆盖三个问题；未伪造响应。
- `archived-link`：记录通知载荷、跳转提示、未打开审批弹窗，以及随后历史接口返回 972099 并在表格可见的对照。
- `task-link-and-sender`：记录同一任务督办通知的 fromUserId、sourceType、sourceId、错误发送人标签及实际错误路由。
- [截图与结构化结果](audit-evidence/2026-09-16/batch-9/)。两个缺陷复现用例通过表示问题存在，不代表已修复。
- 合成通知直接写入隔离库：一条按现有归档通知格式构造；一条按督办表单允许填写的主题和任务关联构造。未向真实用户发送消息。测试点击通知仅更新隔离库中的已读状态。
- 脚本语法、Git 空白检查通过；临时服务及数据库已清理，原有服务与未提交改动保留。

## 复测

按既有 README 启动全新的 `--fixture review` 环境并设置数据库密码环境变量，然后执行：

```powershell
python scripts/ui-audit/seed-batch-8.py --batch 9
node scripts/ui-audit/batch-9-audit.cjs archived-link
node scripts/ui-audit/batch-9-audit.cjs task-link-and-sender
New-Item -ItemType File -Path target/ui-audit/STOP -Force
```

种子脚本校验随机隔离库名，每个全新环境仅加载一次。审核按批次暂停，等待用户选择；全项目审计尚未结束，暂不汇总 TOP3。
