# 双高项目扩展审计任务

基线：后端 `6382090`，上一轮 21 项隔离 HTTP/MySQL 回归通过。保留前后端原有未提交改动。本轮仅审计，不修复业务代码；每批汇报 3～5 条并等待确认。

## 阶段安排

| 阶段 | 检查模块 | 主要检查项 | 状态 |
| --- | --- | --- | --- |
| 1 | 登录、账号、路由、通知 | 身份切换、缓存隔离、表单实际保存、分页、轮询清理、错误兜底 | 首批 5 项已验证，等待用户指令；账户表单和异常场景待继续 |
| 2 | 任务、绩效、成果 | 表单校验、重复点击、上传失败、抽屉竞态、返回/刷新、空态 | 待检查 |
| 3 | 审批及后端服务 | 多角色状态流转、异常回滚、对象权限、并发更新、数据一致性 | 待检查 |
| 4 | 预算、督办、统计与布局 | 真实请求与页面结果一致性、窄屏、样式冲突、渲染及性能 | 待检查 |
| 5 | 汇总 | 核对证据及重复项，汇总 TOP3 风险 | 待前面阶段完成 |

角色覆盖：管理员、普通填报人、无相关任务的另一用户、部门负责人、审核人、成果上传账号。优先真实 HTTP 与独立 MySQL 测试库，Playwright 检查可见页面，延迟请求仅用于复现慢网络和请求交错。

## 跳过事项

用户已忽略：任务/成果详情可见性、静态附件及下载权限、已归档成果编辑/删除、历史审核四级值展示、预算非法金额转零、预算锁定后导入覆盖。本轮不再次提报这些问题。

已修复：账号密码返回与删除账号鉴权、任务管理权限、实际填报值、四级重提绩效刷新、跨年度恢复、撤回边界与事务、绩效审批状态、年度启用、审批结果缺失、并发重复提交。

## 工具与数据边界

- Browser 插件完成连接排查后返回无可用浏览器，使用独立 Playwright + 本机 Edge 的临时上下文。
- 浏览器访问 `127.0.0.1:15173`，后端为 `127.0.0.1:18080`，仅连接随机 `biz_review_test_*` 库。现有 5173/8080 服务保留。
- `scripts/ui-audit/serve.py` 管理服务及测试库，`target/ui-audit/STOP` 触发清理；自动调度关闭，文件及日志写入独立工作目录。
- 合成账号密码只用于临时测试；本地 MySQL 密码由进程环境变量传入，不写入报告、源码或 Git。

## 发现与证据

### 第五批：前端基础交互（均已复现，尚未修复）

| 编号 | 等级/类型 | 位置 | 实测结果、影响 | 建议 |
| --- | --- | --- | --- | --- |
| 5-1 | 高 / BUG | `biz/src/components/Works.vue:1130`、`:2154`；`Home.vue:540` | 同一标签页退出 A 后登录 B，B 会读取 A 的任务缓存并显示任务和部门；B 的真实 `/biz/tasks` 返回空数组，响应后才清除这些数据。存在跨账号内容暴露。 | 缓存按用户隔离，退出/认证失效时清理，并防止旧请求写回其他账号缓存。 |
| 5-2 | 中 / BUG | `Notice.vue:29`、`:57`、`:196`、`:210` | 配置每页 10 条，15 条通知却在第 1、2 页全部重复显示；筛选到 5 条已读后仍显示 2 页。分页和筛选页数失真。 | 增加实际页码及切片，按筛选结果计算总数，筛选后重置页码。 |
| 5-3 | 中 / BUG | `Works.vue:2154`、`:2189` | 首次访问 `/home/works?taskId=930002` 正常打开详情；缓存有效时刷新同一地址，任务列表刷新完成后仍不打开详情。通知/收藏的任务链接失去定位作用。 | 缓存和请求加载共用路由定位逻辑，数据就绪后统一打开目标任务。 |
| 5-4 | 中 / BUG | `Home.vue:408` | 只有 1 条待审绩效时，审核中心显示“1 待处理”，侧栏审核中心没有待办数量；布局只调用任务待办接口。用户可能漏看绩效审核。 | 与审核中心共用待办统计，汇总当前用户可处理的任务、绩效和成果。 |
| 5-5 | 中 / BUG | `Home.vue:504`、`:514` | 首次进入时每 30 秒请求通知 1 次；进入大屏再返回后，同一间隔请求 2 次。Home 未卸载轮询，反复往返会累积请求及已卸载组件更新。 | 保存定时器句柄，卸载时清理，必要时取消未完成请求。 |

证据目录：[截图与结构化结果](audit-evidence/2026-09-16/batch-5/)。5 个复现用例的断言均通过，意味着**成功复现现有缺陷，不代表缺陷已修复**。

- 缓存：`cache.json`、`cache-cross-account.png`，采用延迟真实响应模拟慢网络，没有伪造任务列表。
- 分页：`notices.json`、`notices-page2.png`、`notices-filter-pagination.png`。
- 链接：`deep-link.json`、`deep-link-cold.png`、`deep-link-cached.png`。
- 待办：`badge.json`、`audit-count-missing-performance.png`，使用合成的有效待审绩效。
- 轮询：`polling.json`，Playwright 虚拟时钟推进 30 秒间隔，记录真实发出的 HTTP 请求数。

### 后续验证线索

- 隐藏的个人中心路由仍显示模拟资料和仅提示成功的密码修改：入口已隐藏，需先确认是否属于正式支持页面，再决定是否作为交付缺陷。
- 大屏快速进入后返回时捕获 `Initialize failed: invalid dom.`；此次处于虚拟时钟测试场景，需用普通时序复验，暂不列入第五批。
- 绩效详情异步加载缺少当前对象检查、审核中心分页接线、移动端列表与弹窗适配，进入后续阶段验证。

以上仅为待确认/待复现线索，不计入已确认 BUG 数量。全部阶段完成后才汇总 TOP3。

## 本次验证记录

使用 Playwright 1.63.0、本机 Edge 无头浏览器和 1440×1000 视口；执行 `cache`、`notices`、`deep-link`、`badge`、`polling` 五个浏览器复现场景。仅新增审计脚本、合成数据及文档，没有修改业务实现。测试服务和临时数据库已按脚本清理。
