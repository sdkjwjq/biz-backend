# 双高项目扩展审计任务

基线：后端 `6382090`，上一轮 21 项隔离 HTTP/MySQL 回归通过。保留前后端原有未提交改动。本轮仅审计，不修复业务代码；每批汇报 3～5 条并等待确认。

## 阶段安排

| 阶段 | 检查模块 | 主要检查项 | 状态 |
| --- | --- | --- | --- |
| 1 | 登录、账号、路由、通知 | 身份切换、缓存隔离、表单实际保存、分页、轮询清理、错误兜底 | 首批 5 项已验证，等待用户指令；账户表单和异常场景待继续 |
| 2 | 任务、绩效、成果 | 表单校验、重复点击、上传失败、抽屉竞态、返回/刷新、空态 | 第六批已验证绩效竞态及零值显示；第七批已验证成果数量、必填与文件格式校验；其他检查项待继续 |
| 3 | 审批及后端服务 | 多角色状态流转、异常回滚、对象权限、并发更新、数据一致性 | 第七批已验证审核中心分页及历史记录切换；后端状态与并发等检查待继续 |
| 4 | 预算、督办、统计与布局 | 真实请求与页面结果一致性、窄屏、样式冲突、渲染及性能 | 第六批已验证大屏卸载异常；其他检查项待继续 |
| 5 | 汇总 | 核对证据及重复项，汇总 TOP3 风险 | 待前面阶段完成 |

角色覆盖：管理员、普通填报人、无相关任务的另一用户、部门负责人、审核人、成果上传账号。优先真实 HTTP 与独立 MySQL 测试库，Playwright 检查可见页面，延迟请求仅用于复现慢网络和请求交错。

## 跳过事项

用户已忽略：任务/成果详情可见性、静态附件及下载权限、已归档成果编辑/删除、历史审核四级值展示、预算非法金额转零、预算锁定后导入覆盖。本轮不再次提报这些问题。

已修复：账号密码返回与删除账号鉴权、任务管理权限、实际填报值、四级重提绩效刷新、跨年度恢复、撤回边界与事务、绩效审批状态、年度启用、审批结果缺失、并发重复提交。

## 工具与数据边界

- Browser 插件完成连接排查后返回无可用浏览器，使用独立 Playwright + 本机 Edge 的临时上下文。
- 浏览器访问 `127.0.0.1:15173`，后端为 `127.0.0.1:18080`，仅连接随机 `biz_review_test_*` 库。现有 5173/8080 服务保留。
- `scripts/ui-audit/serve.py` 管理服务及测试库，`target/ui-audit/STOP` 触发清理；自动调度关闭，文件及日志写入独立工作目录。
- 合成账号密码只用于临时测试；本地 MySQL 密码由进程环境变量传入，不写入报告、源码或 Git。

## 发现与证据

### 第五批：前端基础交互（均已复现，尚未修复）

| 编号 | 等级/类型 | 位置 | 实测结果、影响 | 建议 |
| --- | --- | --- | --- | --- |
| 5-1 | 高 / BUG | `biz/src/components/Works.vue:1130`、`:2154`；`Home.vue:540` | 同一标签页退出 A 后登录 B，B 会读取 A 的任务缓存并显示任务和部门；B 的真实 `/biz/tasks` 返回空数组，响应后才清除这些数据。存在跨账号内容暴露。 | 缓存按用户隔离，退出/认证失效时清理，并防止旧请求写回其他账号缓存。 |
| 5-2 | 中 / BUG | `Notice.vue:29`、`:57`、`:196`、`:210` | 配置每页 10 条，15 条通知却在第 1、2 页全部重复显示；筛选到 5 条已读后仍显示 2 页。分页和筛选页数失真。 | 增加实际页码及切片，按筛选结果计算总数，筛选后重置页码。 |
| 5-3 | 中 / BUG | `Works.vue:2154`、`:2189` | 首次访问 `/home/works?taskId=930002` 正常打开详情；缓存有效时刷新同一地址，任务列表刷新完成后仍不打开详情。通知/收藏的任务链接失去定位作用。 | 缓存和请求加载共用路由定位逻辑，数据就绪后统一打开目标任务。 |
| 5-4 | 中 / BUG | `Home.vue:408` | 只有 1 条待审绩效时，审核中心显示“1 待处理”，侧栏审核中心没有待办数量；布局只调用任务待办接口。用户可能漏看绩效审核。 | 与审核中心共用待办统计，汇总当前用户可处理的任务、绩效和成果。 |
| 5-5 | 中 / BUG | `Home.vue:504`、`:514` | 首次进入时每 30 秒请求通知 1 次；进入大屏再返回后，同一间隔请求 2 次。Home 未卸载轮询，反复往返会累积请求及已卸载组件更新。 | 保存定时器句柄，卸载时清理，必要时取消未完成请求。 |

证据目录：[截图与结构化结果](audit-evidence/2026-09-16/batch-5/)。5 个复现用例的断言均通过，意味着**成功复现现有缺陷，不代表缺陷已修复**。

- 缓存：`cache.json`、`cache-cross-account.png`，采用延迟真实响应模拟慢网络，没有伪造任务列表。
- 分页：`notices.json`、`notices-page2.png`、`notices-filter-pagination.png`。
- 链接：`deep-link.json`、`deep-link-cold.png`、`deep-link-cached.png`。
- 待办：`badge.json`、`audit-count-missing-performance.png`，使用合成的有效待审绩效。
- 轮询：`polling.json`，Playwright 虚拟时钟推进 30 秒间隔，记录真实发出的 HTTP 请求数。

### 第六批：业务页面与异步状态（6-1 已修复，其余待修复）

6-1 修复记录、闭环结果和回滚方式见 [第六、七批修复交付](fix-batches-6-7.md)。以下保留原始缺陷描述与证据。

| 编号 | 等级/类型 | 位置 | 实测结果、影响 | 建议 |
| --- | --- | --- | --- | --- |
| 6-1 | 高 / BUG | `biz/src/components/Performance.vue:1802`、`:1827`、`:1160` | 先打开绩效 A，再切到 B，A 的迟到请求覆盖 B 的审核记录。B 的标题下实际显示 A 的审核单；点击“确认提交”后真实请求提交 A 的单号 971001，A 状态从 10 变为 20，B 仍为 10。会审核错误的绩效。 | 切换对象时清空旧审核状态；响应按绩效 ID、年份和请求序号校验，丢弃过期响应；提交前校验审核单归属当前详情。 |
| 6-2 | 中 / BUG | `biz/src/components/Audit.vue:70`、`:396` | 真实接口返回本次填报值 0，审核列表却显示“-”；同页非零值 3 显示正常。审核人无法区分明确填零与未填报。 | 归一化和渲染仅将 null/undefined 视为空值，保留合法的 0。 |
| 6-3 | 低 / BUG | `biz/src/views/Dashboard.vue:619`、`:629`、`:637` | 普通时钟下延迟大屏请求，返回任务页并确认大屏卸载后放行响应，仍触发图表初始化，产生未捕获异常 `Initialize failed: invalid dom.`。已离开的页面继续执行初始化；未观察到整个任务页崩溃。 | 卸载时取消请求及延迟任务，异步返回后检查组件仍挂载，再初始化图表。 |

证据目录：[截图与结构化结果](audit-evidence/2026-09-16/batch-6/)。使用真实本地 HTTP、随机隔离 MySQL 库和合成数据，没有伪造接口响应内容。

- `performance-race.json`：记录 B 的详情、审核单从 971002 变成 971001、实际提交单号及 A/B 审核后的状态；截图记录切换前后与错误审批结果。截图受抽屉滚动区域限制，审核单及状态变化以结构化结果为准。
- `zero-audit.json`、`zero-audit.png`：接口零值、页面“-”及非零对照。
- `dashboard-unmount.json`、`dashboard-after-leaving.png`：页面卸载完成后发生的异常及堆栈，将第五批待复验线索确认为 BUG。
- `performance-year.json`、`performance-year.png`：当前年份输入框实际为 2026，禁用状态正确，填报按钮可用；该显示疑点已排除，不作为问题提报。

本批只增加审计数据、脚本和证据，没有修复业务代码。上述 3 个缺陷复现场景及 1 个正常对照检查均完成。阶段 2～4 仍有未覆盖项，等待用户选择继续检查或展开当前问题。

验证收尾：JavaScript/Python 语法及 `git diff --check` 通过；临时数据库已删除，15173/18080 已停止，原有 5173/8080 服务仍在运行。

### 第七批：审核中心与成果表单（均已复现，尚未修复）

| 编号 | 等级/类型 | 位置 | 实测结果、影响 | 建议 |
| --- | --- | --- | --- | --- |
| 7-1 | 中 / BUG | `biz/src/components/Audit.vue:745` | 点击“审批记录”只筛选已有待办数据，没有请求历史接口。有 1 条已归档记录的账号看到空列表，点击刷新后才显示该记录。正常切换无法查看已有记录，容易误以为历史数据丢失。 | 切换标签时加载对应接口，清理旧列表，并防止旧标签请求覆盖当前列表。 |
| 7-2 | 中 / BUG | `biz/src/components/Audit.vue:103`、`:736` | 每页配置 10 条，接口返回 15 条待办，第 1、2 页却都显示同样的 15 条。页码变化无效，数据量增加时列表也不会分页。与第五批通知分页发生在不同组件。 | 绑定当前页，按筛选结果切片或接入后端分页，筛选及切换标签时重置页码。 |
| 7-3 | 中 / BUG | `biz/src/components/Achievement.vue:298`、`:1509`；`biz-backend/src/main/java/org/example/entity/BizAchievement.java:44`、`service/AchievementService.java:127` | 成果一等奖数量输入 1.5，失焦后仍显示 1.5；页面实际发送 1.5，接口提示新增成功，但再次查询及列表显示为 1。前端允许小数，后端整数绑定静默截断且未拒绝非法数量，造成保存值与输入不一致。 | 前端限制正整数；后端明确拒绝小数并校验范围，避免自动截断。调整反序列化时注意其他接口兼容性。 |

证据目录：[截图与结构化结果](audit-evidence/2026-09-16/batch-7/)。Playwright + 本机 Edge，1440×1100；真实 HTTP 与独立 MySQL 库，未模拟成功响应，未修改业务实现。

- `history-tab.json`：账号初始绩效待办为 0，切换历史时没有请求记录接口；手动刷新后接口返回已归档审核单 972099，列表由 0 行变 1 行。两张截图记录刷新前后。
- `pagination.json`、`audit-page2.png`：真实接口 15 条，当前页码为 2，但两页的 15 行文本完全相同。
- `achievement-count.json`、`achievement-count-before.png`、`achievement-count-after.png`：浏览器输入/请求值为 1.5；按本次新增返回 ID 读取保存后的数量为 1，页面显示“一等奖×1”。
- `achievement-validation.json` 及截图：空表单出现 6 项必填提示且未调用新增接口；非法 `.txt` 文件被拒绝并从待上传列表清除；合法整数 2 提交、查询、页面显示一致。非法文件列表的清除是异步的，用例等待清除完成后断言。

本批完成 3 个缺陷复现场景及 1 个正常行为对照场景。未执行修复；后续覆盖项仍按阶段安排保留。

验证收尾：JavaScript/Python 语法检查通过；临时数据库已删除，15173/18080 已停止，原有 5173/8080 服务保持运行。原有前后端未提交修改保留。

### 后续验证线索

- 隐藏的个人中心路由仍显示模拟资料和仅提示成功的密码修改：入口已隐藏，需先确认是否属于正式支持页面，再决定是否作为交付缺陷。
- 移动端列表与弹窗适配，进入后续阶段验证。
- 成果退回后的业务规则待确认：当前页面详情字段禁用，源码未提供原单重提入口；需要明确是否要求原单修改重提，或允许重新新增。未把业务意图不明确的事项列为本批 BUG。
- 审核中心历史记录刷新后，已归档绩效可能被计入“待处理”数量；已看到页面线索，后续单独核对不同业务状态及标签往返行为，避免与标签切换问题重复提报。

以上仅为待确认/待复现线索，不计入已确认 BUG 数量。全部阶段完成后才汇总 TOP3。

## 第五批验证记录

修复状态补充：6-2、7-1、7-2、7-3 已完成修复及本地闭环，包含历史记录不计入待办及数量拒绝小数的验证；详情见 [第六、七批修复交付](fix-batches-6-7.md)。上文缺陷描述和原始证据作为审计历史保留。6-3 仍待实施。

使用 Playwright 1.63.0、本机 Edge 无头浏览器和 1440×1000 视口；执行 `cache`、`notices`、`deep-link`、`badge`、`polling` 五个浏览器复现场景。仅新增审计脚本、合成数据及文档，没有修改业务实现。测试服务和临时数据库已按脚本清理。
