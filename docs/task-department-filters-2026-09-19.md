# 任务双部门筛选

- 增加“归口部门”，按可见三级任务的 `deptId` 筛选，部门选项来自当前权限范围内的任务。
- 原“处理部门”明确命名为“当前处理部门”，继续按待审处理人所属部门筛选。
- 归口部门排列在当前处理部门之前，两个条件与年度、状态、任务范围、待审阶段、搜索取交集；保留匹配任务的上级目录。
- 所有条件保持可见，空间不足采用两行，更窄屏幕继续自然换行。取消折叠审核条件的“更多筛选”。
- 新条件接入会话记忆、重置、详情链接及清除筛选定位，不修改后端及权限。

## 验证与回滚

- `task-filter-layout.cjs`：5 组 Playwright 检查通过，浏览器异常 0；包含归口部门独立筛选、归口部门 A 与当前处理部门 B 组合、空结果刷新、重置及 5 种屏宽。
- 修改文件 ESLint 和前端构建通过，保留原有大包体积提醒。
- 原客户反馈 Playwright 5 组回归再次通过；前端提交：`877cb77`。
- 证据：[截图与结果](evidence/task-department-filters/)。
- 仅需更新前端构建；回滚本次前端提交后重新构建，或恢复上一版前端产物，无需数据库操作。
- 原有成果页、审核中心及 Vite 未提交修改保持原样。
