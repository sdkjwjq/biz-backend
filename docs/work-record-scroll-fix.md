# 工作纪实下半部分无法选择修复

原因：Home内容区采用overflow:hidden，各业务页面需要独立滚动；纪实页面遗漏height与overflow，内容超出可视区后被裁剪。吸底操作栏进一步遮挡改革任务选择框。此前测试使用元素自动滚入视口，没有验证真实鼠标滚轮，未覆盖这个问题。

修复：仅为WorkRecords页面增加100%高度、border-box及纵向滚动，并将列表／详情切换时的滚动复位改为重置自身scrollTop。没有更改主布局或业务数据。

新增Playwright回归 `scripts/ui-audit/work-record-scroll.cjs`：使用随机隔离库，真实登录和创建草稿，将详情展示模拟为22条任务；用mouse.wheel滚动，要求选择框不被底栏覆盖，实际选择改革任务，填写进展和最下方其它事项，检查返回列表后滚动复位。覆盖1440×600、1440×900、768×700三种窗口；修复前滚轮断言失败，修复后三种窗口通过、浏览器异常0。证据见 `evidence/work-record-scroll/`。

前端ESLint和生产构建通过。本地手测5174读取最新源码，刷新页面即可生效；交付包中的frontend-dist.zip同步更新。无需后端更新或数据库迁移；回滚本次前端提交即可撤销布局修复。
