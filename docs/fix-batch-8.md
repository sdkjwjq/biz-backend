# 第八批修复：8-1、8-3

用户选择：8-2（无趋势数据时显示示例曲线）忽略；修复 8-1、8-3 后直接继续审核。原有未提交改动保留。

| 问题 | 前端提交 | 修复 |
| --- | --- | --- |
| 8-1 预算月份数据错配 | `98797f9` | 请求捕获年月及序号；仅当前请求更新表格、错误和 loading。切换清空旧数据，加载中禁用锁定；锁定操作的迟到响应同样校验当前年月。卸载使请求失效。 |
| 8-3 大屏年份串用 | `1185535` | 一轮加载共享年份、类型和序号快照；任务、部门、趋势、成果的异步结果和失败回调均校验当前上下文；过期请求不能更新状态或发起绘图。初次加载及延迟初始化沿用同样判断。 |

## 本地闭环

- `batch-8-audit.cjs budget-race --fixed`：延迟 1 月后切到 2 月，放行旧响应后仍为 222.00、未锁定，当前月份保持 2 月。
- `batch-8-audit.cjs year-race --fixed`：2027 年正确显示 33%；迟到的 2025 年 77% 不覆盖，图表动画结束后仍为 33%。
- `dashboard-lifecycle.cjs delayed-init`、`repeat`：延迟初始化退出和三轮重复进出回归通过，重进均有五个图表，无浏览器异常。
- `npm.cmd run build` 通过，原有大资源包提示保留。Dashboard ESLint 通过；Budget ESLint 有两处既有 `no-unused-vars`（escapeHtml、buildExportRows），使用 HEAD 基线复核为同样两项，本轮未新增 lint 问题，未擅自删除原函数。
- 脚本语法、Git 空白检查通过。此次没有后端业务修改，未重复执行上一轮已通过的 23 项后端回归。

证据：[修复截图与结果](fix-evidence/batch-8/)。使用合成数据、真实 HTTP 与随机隔离 MySQL 库；原始缺陷证据保留不变。`--fixed` 表示正确行为断言，未带该参数的原复现断言用于历史缺陷验证。

## 部署与回滚

最新前端产物在 `biz/dist`，尚未部署。无需后端更新、数据库迁移或回滚 SQL；第六七批旧 ZIP 保留原版本，不能代表本轮最新前端。

分别反向提交 `git revert 1185535`、`git revert 98797f9`，或恢复此前前端产物；保留用户未提交改动，不使用硬重置。
