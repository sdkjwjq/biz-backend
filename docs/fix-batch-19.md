# 第十九批修复：任务、绩效并发状态流转

## 改动与提交

用户确认修复 19-1、19-2、19-3。预算模块及已忽略事项不涉及，原有未提交工作保留。

- **19-1，`87ed122`**：任务审核复用现有 `getAuditBySubIdIncludingDeletedForUpdate`，获取行锁后检查逻辑删除标记，再沿用处理人与状态校验；任务撤回新增最新审核单的锁定查询，避免与审核或重复撤回同时使用旧状态。保留普通读取接口。
- **19-2、19-3，`384b7ee`**：新增绩效审核单锁定查询，审核和撤回均在已有事务内先取得该行锁，再校验处理人、逻辑删除与当前状态。两个入口共用锁定规则，因此作为同一修复提交。只有通过校验的操作继续恢复快照或写入结果、日志、通知。

沿用原架构、错误结构、状态码、材料保留及成果退回重提规则；不新增接口参数，不修改数据库结构。锁仅在当前事务内持有，事务完成或回滚后释放。按最新状态继续原流程判断，不新增客户端请求幂等键，也不改变同一人合法处理不同阶段的规则。

## 本地闭环

新增脚本 `scripts/ui-audit/batch-19-fixed.py`，原始 `batch-19-audit.py` 和审计证据保留。

**10 个定向场景全部通过：**

1. 任务通过＋退回。
2. 任务重复通过。
3. 任务重复退回。
4. 任务重复撤回。
5. 绩效重复通过。
6. 绩效通过＋退回（本次未出现审计时的数据库死锁）。
7. 绩效重复退回。
8. 绩效归档与撤回同时竞争。
9. 撤回先开始，阻塞在年度值恢复时再发归档：归档等待，撤回完成后归档被拒绝，没有归档通知。
10. 归档先完成再撤回：撤回被拒绝，归档状态 30 和实际值 3 保留。

前九项为真实 HTTP/MySQL 并发，利用独立事务行锁延迟，确认请求确实进入锁等待；第十项为串行边界。核对审核单状态、逻辑删除、任务实际值、绩效年度值和总值，以及日志、通知数量；另核对任务及绩效原审核人的重复请求被拒绝且日志未增加。

现有隔离回归 **31 项通过，失败 0，错误 0**；后端 `mvn.cmd -B -DskipTests package` 成功，Python 语法和 Git 差异检查通过。此次仅改后端，未运行浏览器检查或前端构建。

[定向结果](fix-evidence/batch-19/results.json) · [回归摘要](fix-evidence/batch-19/backend-regression.txt)

复跑：设置 `SHUANGGAO_TEST_DB_PASSWORD` 后执行 `python scripts/run-review-regression.py`；启动 `python scripts/ui-audit/serve.py --fixture review`，再执行 `python scripts/ui-audit/batch-19-fixed.py`。结束时创建 `target/ui-audit/STOP`，等待服务停止并删除随机库。脚本仅接受随机 `biz_review_test_*` 数据库，全部样本均为合成数据。本次回归库和定向测试库均已清理，临时服务已停止。

## 部署与回滚

未部署。后端产物为 `target/biz_backend-1.0-SNAPSHOT.jar`，按现有部署方式更新并重启；无需前端更新或 SQL。

任务修复回滚：`git revert 87ed122`；绩效修复回滚：`git revert 384b7ee`。重新打包后按现有流程发布，或恢复上一版后端产物。回滚会恢复本批并发缺陷；不使用硬重置，不影响既有未提交工作。
