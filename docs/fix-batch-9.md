# 第九批三项修复

本轮仅修改前端审核中心和消息中心，沿用现有接口与权限范围；原有未提交改动保留。

| 问题 | 前端提交 | 改动 |
| --- | --- | --- |
| 9-1 归档通知无法定位 | `a7be8b7` | 当前列表未命中时加载另一标签的完整数据，按业务类型及单号定位；历史单以只读详情打开。路由快照和序号阻止旧定位在切换链接、标签或卸载后弹窗。 |
| 9-2 标题误导业务跳转 | `1ee9c87` | 明确的数值 0/1、2、3 及 task/performance/achievement 类型优先分流；任务审核仍按任务 ID 进入审核中心，其他任务消息进入工作中心。旧格式保留标题兼容判断。 |
| 9-3 发送人固定显示管理员 | `a3f9126` | 保存真实 fromUserId，使用现有用户目录映射昵称/账号名称；目录不可用或账号不在目录时显示“用户＋账号”，缺少发送人时显示“未知发送人”，不再错误标为系统管理员。 |

## 本地闭环

- `batch-9-audit.cjs archived-link --fixed`：实际点击归档通知，打开审核单 972099 的“详情查看”，无“确认提交”按钮。
- `batch-9-audit.cjs task-link-and-sender --fixed --sender-fixed`：实际发送人 910003 显示“审计审核人”；含“成果”的任务通知跳到 `/home/works?taskId=930002`。此测试验证正确路由，未宣称修复第五批任务缓存导致的详情未自动打开问题。
- 同上增加 `--sender-fallback`：明确模拟用户目录 HTTP 503，通知仍可加载，发送人显示“用户910003”，业务跳转正确。其他接口使用真实响应。
- `audit-center-regression.cjs deep-link`：已有待办单 972013（第一页之外）仍可从通知链接打开审批弹窗。
- Audit.vue、Notice.vue ESLint，前端构建，修改脚本语法及 Git 空白检查通过。原有大资源包提示保留。没有后端业务改动，未重复执行已通过的 23 项后端回归。

证据：[修复结果与截图](fix-evidence/batch-9/)。Playwright + Edge、真实 HTTP、随机隔离 MySQL 库，全部为合成数据；用户目录失败场景明确标注。原始缺陷证据保留。

## 交付与回滚

最新前端构建产物位于 `biz/dist`，尚未部署；旧第六七批 ZIP 不代表当前最新前端。无需更新后端、迁移数据库或回滚 SQL。

按新到旧分别执行 `git revert a3f9126`、`git revert 1ee9c87`、`git revert a7be8b7`，或恢复此前前端产物；保留原有未提交工作，不使用硬重置。

临时服务和隔离库已清理，原有 5173/8080 服务保留。
