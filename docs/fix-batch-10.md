# 第十批 10-1 修复与闭环

## 修改

仅修改前端 `src/components/Works.vue`，保持原接口、业务状态码和数据库结构。

- 任务详情使用任务 ID 和打开序号校验。切换、关闭、卸载使旧上下文失效，打开或关闭时清空旧审核、日志和子任务数据。
- 四级任务、审核单、审核日志及通知兜底的异步回调检查当前上下文。审核刷新另有请求序号，旧结束回调不能提前解除新请求的 loading。
- 审核单必须属于当前任务；加载中禁止审批，提交前再次校验任务归属、当前处理人和重复提交状态。
- 固定提交时的单号、审批结果和意见；返回时只有原详情仍有效才能更新其表单和审核数据。

## 本地测试

真实隔离 MySQL、HTTP、Playwright + Edge，全部使用合成数据。

1. `batch-10-audit.cjs --fixed`：延迟 A 的真实审核响应，关闭后打开 B，再放行 A；页面保持 B，实际提交 B 单号 981002，B 从 10 到 20，A 保持 10。
2. `task-detail-lifecycle.cjs`：旧审核成功、旧审核 503、旧四级任务响应三种情况下，B 不出现 A 的审批按钮；关闭 B 再打开 A，审批正常可用。
3. 同脚本覆盖请求未完成时离开任务页面；旧响应返回后没有重新打开抽屉或浏览器异常。
4. Works.vue ESLint、前端生产构建、修改文件空白检查及两个测试脚本语法检查通过。构建保留现有大分包提示。

共 1 个真实审批用例和 4 个生命周期场景通过。[测试证据](fix-evidence/batch-10/)。原始缺陷证据保持不变。

## 复测

启动全新的 `serve.py --fixture review` 隔离环境，按既有方式设置数据库密码环境变量后执行：

```powershell
python scripts/ui-audit/seed-batch-8.py --batch 10
node scripts/ui-audit/batch-10-audit.cjs --fixed
node scripts/ui-audit/task-detail-lifecycle.cjs
New-Item -ItemType File -Path target/ui-audit/STOP -Force
```

生命周期测试依赖前一测试完成后 A=10、B=20 的合成状态。测试结束后已清理临时服务和随机数据库。

## 交付与回滚

前端修复提交：`8c4869c`；回滚命令（在前端仓库）：`git revert 8c4869c`。

前端产物为 `biz/dist`，本次未部署。只需更新前端；后端和数据库无需发布或迁移。回滚采用本次前端提交的 `git revert`，重新构建后发布，或恢复前一版前端产物。原有未提交改动不纳入本次提交。

本次没有后端业务改动，因此未重复运行后端 23 项回归或打包。用户已忽略事项、其他模块疑点均未改动。
