# 工作纪实导出表格字体

两张表的表头、编号、任务名称、负责人、状态及填报文字统一使用 `仿宋_GB2312`、小四（12磅），显式覆盖中文、英文、数字和复杂文字字符集。封面、表格外标题和正文保持原样。

POI新增行会复制XML，所以正文行必须在插入前设置字体；表头单独应用相同格式。回归测试重新打开实际生成的合并DOCX，逐个检查全部表格的所有文字段的四种字符集字体及字号，防止只修改内存对象却未写入文件。

验证：20项纪实隔离测试通过，后端打包通过。Word实际打开和分页通过：长文本单份11页，合并21页，跨页表格重复表头、每份独立封面均保留。证据见 `evidence/work-record-table-font/`。

本地5174对应的18081后端已更新，重新点击导出即可获取新格式；之前下载的Word不会自动改变。交付JAR及校验和同步更新。本次无需数据库迁移。可反向提交本次改动或恢复 `target/manual-work-records/backend-before-font-*.jar` 中本次更新前的备份后重启服务来回滚。
