# 第二十批修复记录

- **20-1 / `68e29a7`**：一键完结先锁定最新审核单，再锁定并读取最新任务，沿用原状态校验；避免此前查询用户所建立的事务快照影响任务状态判断。
- **20-2 / `74d95f7`**：成果新增、修改共用已有类别、名称、级别、颁发时间必填校验；不改变完整更新接口或归档成果编辑规则。
- **20-3 / `40e6dff`**：账号新增、修改及改密共用既有非空、非纯空白、至少六位规则。保留合法密码两端空格，不自动裁剪；完整修改接口未传密码仍失败，不引入部分更新语义。沿用现有 IllegalArgumentException 对应 HTTP 400/ErrorVO 响应。

## 验证

`scripts/ui-audit/batch-20-fixed.py` 在合成数据、随机隔离库中验证三项全部通过：

- 两个真实并发一键完结仅一个成功，审批链一套、完成通知一条；归档后撤回被拒绝。
- 新增/修改成果共 12 个非法输入被拒绝，成果、审核单、日志和通知无变化；合法修改保存成功。
- 账号新增/修改共 10 个非法密码被拒绝，新增无落库、修改原账号保持不变且旧密码可登录；合法密码创建、修改及登录成功，两端空格保持。
- 现有隔离回归 31 项全部通过；后端打包、Python 语法及 Git 差异检查通过。

初次定向脚本未处理账号校验的既有 HTTP 400 响应，已补上并在全新隔离库完整复测通过。未将脚本错误计为产品缺陷。本批无前端改动，未重复浏览器验证。

[定向证据](fix-evidence/batch-20/results.json) · [回归摘要](fix-evidence/batch-20/backend-regression.txt)

复跑：设置 `SHUANGGAO_TEST_DB_PASSWORD`，先运行 `python scripts/run-review-regression.py`；启动 `python scripts/ui-audit/serve.py --fixture review`，执行 `python scripts/ui-audit/batch-20-fixed.py`。结束创建 `target/ui-audit/STOP` 并等待随机库清理。原始缺陷脚本保留。

## 部署及回滚

未部署；产物 `target/biz_backend-1.0-SNAPSHOT.jar`，沿用现有后端发布流程，无数据库迁移、无需前端更新。按需 `git revert` 对应修复提交后重新打包，或恢复上一版本产物；不使用硬重置，不影响原有未提交工作。

用户已要求修复后自动继续审计，因此本批闭环完成后继续下一批检查，不自动修复新发现的问题。
