# 第六、七批最终本地交付

## 构建产物

产物位于本仓库 `target/release-batches-6-7/`（本地生成，不提交二进制）：

- `frontend.zip`：将内容解压至现有静态站点目录，ZIP 根目录为 index.html 和 assets。
- `biz_backend-1.0-SNAPSHOT.jar`：后端 Spring Boot 产物。
- `SHA256SUMS.txt`：两项产物校验和。

对应前端 HEAD `a181c56`，后端业务修复 `501b985`。产物基于当前工作区，包含原有未提交的 Achievement.vue、Audit.vue、vite.config.js 改动；本轮提交只包含此次修复。后端原有未提交测试改动保留，未运行这些非隔离测试。

## 部署顺序

1. 保存当前前端目录、后端 JAR 和现有启动参数，以便恢复。
2. 先更新后端 JAR，沿用既有服务管理方式和外部配置，保留数据库连接、上传目录、日志目录、端口及反向代理设置。
3. 后端启动后，用获授权的测试账号验证成果整数保存与小数 HTTP 400 响应，再更新前端静态产物；现有前端路由回退和 `/api` 代理保持不变。
4. 前端验证：审核中心零值、历史标签及分页；绩效切换后的审批归属；成果小数拒绝、整数保存；大屏退出重进。生产业务记录不作为随意审批的测试数据。

无需数据库迁移、回填或回滚 SQL。本次没有执行服务器部署或 Git 推送。

## 验证结果

- 23 项后端隔离回归：全部通过（原有 21＋新增 2），数据库自动清理。
- 各模块 Playwright 闭环通过；最终大屏 5 个场景通过。
- 七个修改前端文件 ESLint、前端构建、后端打包和修改脚本语法检查通过。
- 已有前端大资源包提示仍存在。具体结果和截图见 `fix-evidence/batches-6-7/`，汇总见 `fix-batches-6-7.md`。

## 整批回滚

部署回滚优先恢复对应上一版本前端目录与后端 JAR，保留运行配置和上传文件。

若回滚代码，在保留当前未提交改动的前提下，分别反向提交：

```powershell
# biz，按新到旧顺序；遇到冲突保留用户原有改动。
git revert a181c56
git revert b0d6123
git revert db8e86c
git revert 0af492c
git revert 09f0209
git revert 29e05cb

# biz-backend，仅本轮后端业务修复。
git revert 501b985
```

测试证据与文档提交可保留。不使用硬重置，回滚后重新构建并执行对应隔离回归。
